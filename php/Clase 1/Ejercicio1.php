<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Ejercicio 1</title>
  </head>
  <body>
    <?php
      $number=10;
      echo $number;
      var_dump ($number);
      $decimal=5.2;
      echo $decimal;
      var_dump ($decimal);
      $str1='Cadena de caracteres en comillas simples';
      echo $str1;
      var_dump ($str1);
      $str2="Cadena con comillas dobles";
      echo $str2;
      var_dump ($str2);
      $number="A la variable del punto A le asignamos una cadena";
      echo $number;
      var_dump ($number);
      $str1=23.4;
      echo $str1;
      var_dump ($str1);
      $number="10";
      echo $number;
      var_dump ($number);
     ?>
  </body>
</html>
